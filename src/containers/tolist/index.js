import React, { Component } from 'react'
import { Link, withRouter } from 'react-router-dom'
import assign from 'lodash/assign'

class TolistContainer extends Component {
  componentWillMount () {
  }
  render () {
    return (
      <div>
       'tolist'
      </div>
    )
  }
}

export default withRouter(TolistContainer)